import json


    # # save json files with the config parameteres
    # with open(os.path.join(outputs_folder_path, "data.json"), 'w') as f:
    #     f.write(json.dumps(config_data.__dict__))
    
    # with open(os.path.join(outputs_folder_path, "plot.json"), 'w') as f:
    #     f.write(json.dumps(config_plot.__dict__))